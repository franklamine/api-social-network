# schema: https://json.schemastore.org/github-workflow.json

name: deploment du backend de social network

on:
  push:
    branches:
      -main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: recuperation du repository
        uses: actions/checkout@v3

      - name: Message après checkout
        run: echo "Code recupéré avec succès"



